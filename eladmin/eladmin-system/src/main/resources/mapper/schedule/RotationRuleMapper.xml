<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.zhengjie.modules.schedule.mapper.RotationRuleMapper">

    <resultMap id="RotationRuleResultMap" type="me.zhengjie.modules.schedule.domain.RotationRule">
        <id column="rule_id" property="ruleId"/>
        <result column="name" property="name"/>
        <result column="code" property="code"/>
        <result column="type" property="type"/>
        <result column="cycle_days" property="cycleDays"/>
        <result column="pattern" property="pattern"/>
        <result column="enabled" property="enabled"/>
        <result column="remark" property="remark"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="findAll" resultMap="RotationRuleResultMap">
        SELECT r.* FROM schedule_rotation_rule r
        <where>
            <if test="name != null and name != ''">
                AND r.name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="enabled != null">
                AND r.enabled = #{enabled}
            </if>
        </where>
        ORDER BY r.rule_id ASC
    </select>

</mapper>
