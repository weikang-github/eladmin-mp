<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.zhengjie.modules.schedule.mapper.ScheduleMapper">

    <resultMap id="ScheduleResultMap" type="me.zhengjie.modules.schedule.domain.Schedule">
        <id column="schedule_id" property="scheduleId"/>
        <result column="user_id" property="userId"/>
        <result column="shift_id" property="shiftId"/>
        <result column="schedule_date" property="scheduleDate"/>
        <result column="type" property="type"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="findAll" resultMap="ScheduleResultMap">
        SELECT s.* FROM schedule s
        <where>
            <if test="userId != null">
                AND s.user_id = #{userId}
            </if>
            <if test="shiftId != null">
                AND s.shift_id = #{shiftId}
            </if>
            <if test="startDate != null">
                AND s.schedule_date >= #{startDate}
            </if>
            <if test="endDate != null">
                AND s.schedule_date <= #{endDate}
            </if>
        </where>
        ORDER BY s.schedule_date ASC
    </select>

    <select id="findByUserIdAndDateRange" resultMap="ScheduleResultMap">
        SELECT s.* FROM schedule s
        WHERE s.user_id = #{userId}
          AND s.schedule_date >= #{startDate}
          AND s.schedule_date <= #{endDate}
        ORDER BY s.schedule_date ASC
    </select>

    <select id="findByDeptIdAndDateRange" resultMap="ScheduleResultMap">
        SELECT s.* FROM schedule s
        LEFT JOIN sys_user u ON s.user_id = u.user_id
        WHERE u.dept_id = #{deptId}
          AND s.schedule_date >= #{startDate}
          AND s.schedule_date <= #{endDate}
        ORDER BY s.schedule_date ASC
    </select>

    <select id="findConflicts" resultMap="ScheduleResultMap">
        SELECT s.* FROM schedule s
        WHERE s.user_id = #{userId}
          AND s.schedule_date = #{scheduleDate}
          <if test="scheduleId != null">
              AND s.schedule_id != #{scheduleId}
          </if>
    </select>

    <insert id="batchInsert">
        INSERT INTO schedule (user_id, shift_id, schedule_date, type, status, create_by, create_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.shiftId}, #{item.scheduleDate}, #{item.type}, #{item.status}, 
             #{item.createBy}, #{item.createTime})
        </foreach>
    </insert>

    <delete id="deleteByDateRange">
        DELETE FROM schedule
        WHERE schedule_date >= #{startDate}
          AND schedule_date <= #{endDate}
          <if test="userId != null">
              AND user_id = #{userId}
          </if>
    </delete>

</mapper>
